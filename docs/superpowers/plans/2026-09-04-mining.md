# 开采 Mining Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 加开采系统（6 矿种 + 煤、挂机矿脉、耐久-再生循环、冶炼成锭），解锁因 `state.oreCount` 恒 0 而全锁的锻造，让「打怪 → 装备 → 挖得快 → 冶炼 → 锻更好装备」闭环转起来。

**Architecture:** 纯数据+逻辑抽进可单测 UMD 模块 `skins/db-console/mining.js`（仿 fishing/combat/equip）；`equip.js` 的配方从 `ore: number` 改为 `bars: {key:n}`；`farm.js` 只做 DOM 与状态，每秒 render tick 里调 `settleMining`，不另开定时器。`mining.js` 不依赖 combat/equip —— 角色属性由调用方算好传入。

**Tech Stack:** 原生 JS、`node --test`、无构建。

配套 spec：`docs/superpowers/specs/2026-09-04-mining-design.md`

---

## 文件结构

- **新建** `skins/db-console/mining.js` — 数据（`ORES`/`BARS`/`BAR_KEYS`/`VEINS`/`VEIN_KEYS`/`COAL_KEY`/`SMELT_XP`/`OFFLINE_CAP`）+ 纯逻辑（`veinUnlocked`/`tickMs`/`yieldPer`/`smeltRecipe`/`smeltCheck`/`settleMining`/`migrateMining`）。UMD 导出 `module.exports` 兼 `window.Mining`。
- **新建** `test/mining.test.js`。
- **改** `skins/db-console/equip.js` — `recipe()` 的 `ore` 换成 `bars`，`recipeCheck()` 的 `oreOk` 换成 `barsOk`，新增 `TIER_BAR`，`migrateEquip()` 移除 `oreCount` 兜底。
- **改** `test/equip.test.js` — 3 处断言随之更新，加一条跨模块 key 一致性断言。
- **改** `server.js:55` — `GAME_FILES` 加 `'/mining.js'`（否则 tracer 皮肤 404）。
- **改** `test/server.test.js:178` — 兜底文件列表加 `'/mining.js'`。
- **改** `skins/db-console/index.html:67` 与 `skins/tracer/index.html:171` — 在 equip.js 之后、farm.js 之前引入 mining.js。
- **改** `test/tracer-skin.test.js:62` — 脚本顺序断言纳入 mining.js。
- **改** `skins/db-console/farm.js` — 引入 Mining；状态字段与迁移；render tick 结算；`TABS` 加 `mine`；`mineTab()`；`forgeTab()` 加熔炉区并解禁锻造；点击分发加 `data-vein`/`data-smelt`/`data-forge`。
- **改** `skins/db-console/farm.css` — 复用 `.hpbar`，只加 `.hpfill.hp-ore`、`.rec-miss`、`.vein-locked`。

约定：`ORES`/`BARS`/`VEINS` 的 key 永不改。不碰 `farm-data.js`。

**跨会话提醒：** `server.js`、`skins/tracer/index.html`、`test/tracer-skin.test.js` 是 tracer 那条线也在改的文件。Task 6 动它们之前先 `git pull` / 确认工作区干净，提交时只 `git add` 明确列出的文件，**绝不 `git add -A`**。

---

## Task 1: mining.js 数据表与基础纯函数

**Files:**
- Create: `skins/db-console/mining.js`
- Test: `test/mining.test.js`

- [ ] **Step 1: 写失败的测试**

创建 `test/mining.test.js`：

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const M = require('../skins/db-console/mining.js');
const C = require('../skins/db-console/combat.js');
const E = require('../skins/db-console/equip.js');

test('7 条矿脉：6 档矿 + 1 条煤脉，key 与 VEIN_KEYS 一致', () => {
  assert.strictEqual(M.VEIN_KEYS.length, 7);
  M.VEIN_KEYS.forEach((k) => assert.ok(M.VEINS[k], k + ' 应在 VEINS 里'));
  assert.strictEqual(Object.keys(M.VEINS).length, 7);
  const tiers = M.VEIN_KEYS.map((k) => M.VEINS[k].tier).sort((a, b) => a - b);
  assert.deepStrictEqual(tiers, [0, 1, 2, 3, 4, 5, 6]);
});

test('矿脉产出的矿石都在 ORES 里，煤脉产煤', () => {
  M.VEIN_KEYS.forEach((k) => {
    const v = M.VEINS[k];
    assert.ok(M.ORES[v.ore], k + ' 的产出 ' + v.ore + ' 不在 ORES 里');
    assert.ok(v.name && v.job, k + ' 缺名/黑话');
    assert.ok(v.tickMs > 0 && v.hp > 0 && v.respawnMs > 0 && v.xp > 0, k + ' 数值非法');
  });
  assert.strictEqual(M.VEINS.burn_seam.ore, M.COAL_KEY);
  assert.strictEqual(M.ORES[M.COAL_KEY].tier, 0);
});

test('6 种锭各对一档矿，档位与矿石 tier 对齐', () => {
  assert.strictEqual(M.BAR_KEYS.length, 6);
  M.BAR_KEYS.forEach((bk) => {
    const b = M.BARS[bk];
    assert.ok(b.name && b.job, bk + ' 缺名/黑话');
    assert.ok(M.ORES[b.ore], bk + ' 的原矿 ' + b.ore + ' 不在 ORES 里');
    assert.strictEqual(M.ORES[b.ore].tier, b.tier, bk + ' 档位应与原矿一致');
  });
  const tiers = M.BAR_KEYS.map((k) => M.BARS[k].tier).sort((a, b) => a - b);
  assert.deepStrictEqual(tiers, [1, 2, 3, 4, 5, 6]);
});

test('矿脉解锁门槛与 combat / equip 的档位三方一致', () => {
  for (let t = 1; t <= 6; t++) {
    const vein = M.VEIN_KEYS.find((k) => M.VEINS[k].tier === t);
    assert.strictEqual(M.VEINS[vein].unlock, C.TIER_BASE[t].unlock, 'T' + t + ' 与 combat 不一致');
    assert.strictEqual(M.VEINS[vein].unlock, E.TIER_UNLOCK[t], 'T' + t + ' 与 equip 不一致');
  }
  assert.strictEqual(M.VEINS.burn_seam.unlock, 1, '煤脉必须开局解锁，否则炼不出第一块锭');
});

test('veinUnlocked：按等级门槛，未知 key 为 false', () => {
  assert.strictEqual(M.veinUnlocked('heap_seam', 1), true);
  assert.strictEqual(M.veinUnlocked('block_void', 30), false);
  assert.strictEqual(M.veinUnlocked('block_void', 31), true);
  assert.strictEqual(M.veinUnlocked('nope', 99), false);
});

test('tickMs：agi 缩短间隔，50% 封顶，未知 key 为 0', () => {
  const base = M.VEINS.heap_seam.tickMs;
  assert.strictEqual(M.tickMs('heap_seam', { agi: 0 }), base);
  assert.strictEqual(M.tickMs('heap_seam', { agi: 100 }), Math.round(base * 0.5));
  assert.strictEqual(M.tickMs('heap_seam', { agi: 9999 }), Math.round(base * 0.5), '上限必须生效');
  assert.strictEqual(M.tickMs('heap_seam', null), base, '无属性时按基线');
  assert.strictEqual(M.tickMs('nope', { agi: 0 }), 0);
});

test('yieldPer：luk 触发双产，50% 封顶', () => {
  assert.strictEqual(M.yieldPer({ luk: 0 }, () => 0), 1, 'luk 0 恒单产');
  assert.strictEqual(M.yieldPer({ luk: 50 }, () => 0.1), 2, '0.1 < 0.2 命中');
  assert.strictEqual(M.yieldPer({ luk: 50 }, () => 0.5), 1, '0.5 > 0.2 未命中');
  assert.strictEqual(M.yieldPer({ luk: 9999 }, () => 0.49), 2, '上限 0.5 内命中');
  assert.strictEqual(M.yieldPer({ luk: 9999 }, () => 0.5), 1, '上限必须生效');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/mining.test.js`
Expected: FAIL，报 `Cannot find module '../skins/db-console/mining.js'`

- [ ] **Step 3: 写最小实现**

创建 `skins/db-console/mining.js`：

```js
(function (root, factory) {
  'use strict';
  var api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.Mining = api;
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  // 煤是通用燃料，不属于任何档位；单独常量，冶炼与 UI 都引用它而非硬编码。
  var COAL_KEY = 'burn_seg';

  // 矿石：key 永不改。tier 0 = 煤。emoji 避开 Combat.MATERIALS 已占用的那几个。
  var ORES = {
    heap_ore:   { name: '铜矿',   job: 'heap_ore',   emoji: '\u{1F7E0}', tier: 1 },
    fsm_ore:    { name: '铁矿',   job: 'fsm_ore',    emoji: '\u{1F529}', tier: 2 },
    vm_ore:     { name: '秘银矿', job: 'vm_ore',     emoji: '\u{1F4A0}', tier: 3 },
    brin_ore:   { name: '精金矿', job: 'brin_ore',   emoji: '\u{1F7E3}', tier: 4 },
    tblspc_ore: { name: '陨铁矿', job: 'tblspc_ore', emoji: '\u{1FAA8}', tier: 5 },
    void_ore:   { name: '虚空矿', job: 'void_ore',   emoji: '\u{1F573}', tier: 6 },
    burn_seg:   { name: '煤',     job: 'burn_seg',   emoji: '\u{2B1B}',  tier: 0 },
  };

  // 锭：ore 指向本档原矿，冶炼配方由此派生。
  var BARS = {
    heap_bar:   { name: '铜锭',   job: 'heap_bar',   emoji: '\u{1F7E7}', tier: 1, ore: 'heap_ore' },
    fsm_bar:    { name: '铁锭',   job: 'fsm_bar',    emoji: '\u{2B1C}',  tier: 2, ore: 'fsm_ore' },
    vm_bar:     { name: '秘银锭', job: 'vm_bar',     emoji: '\u{1F7E6}', tier: 3, ore: 'vm_ore' },
    brin_bar:   { name: '精金锭', job: 'brin_bar',   emoji: '\u{1F7EA}', tier: 4, ore: 'brin_ore' },
    tblspc_bar: { name: '陨铁锭', job: 'tblspc_bar', emoji: '\u{1F7E8}', tier: 5, ore: 'tblspc_ore' },
    void_bar:   { name: '虚空锭', job: 'void_bar',   emoji: '\u{1F7E5}', tier: 6, ore: 'void_ore' },
  };
  var BAR_KEYS = ['heap_bar', 'fsm_bar', 'vm_bar', 'brin_bar', 'tblspc_bar', 'void_bar'];

  // 矿脉：tickMs 挖一次的基线间隔，hp 耐久（挖 hp 次枯竭），respawnMs 枯竭后的再生 CD。
  // unlock 与 Combat.TIER_BASE / Equip.TIER_UNLOCK 的档位门槛严格一致，三处必须同步改。
  var VEINS = {
    burn_seam:   { name: '燃料层',     job: 'burn_seam',   tier: 0, unlock: 1,  ore: COAL_KEY,     tickMs: 3200, hp: 12, respawnMs: 24000,  xp: 2 },
    heap_seam:   { name: '堆表矿脉',   job: 'heap_seam',   tier: 1, unlock: 1,  ore: 'heap_ore',   tickMs: 3000, hp: 10, respawnMs: 20000,  xp: 2 },
    fsm_seam:    { name: '空闲空间图', job: 'fsm_seam',    tier: 2, unlock: 5,  ore: 'fsm_ore',    tickMs: 3600, hp: 12, respawnMs: 30000,  xp: 5 },
    vm_seam:     { name: '可见性图',   job: 'vm_seam',     tier: 3, unlock: 10, ore: 'vm_ore',     tickMs: 4200, hp: 14, respawnMs: 45000,  xp: 10 },
    brin_seam:   { name: 'BRIN 块区',  job: 'brin_seam',   tier: 4, unlock: 16, ore: 'brin_ore',   tickMs: 5000, hp: 16, respawnMs: 70000,  xp: 18 },
    tblspc_deep: { name: '表空间深层', job: 'tblspc_deep', tier: 5, unlock: 23, ore: 'tblspc_ore', tickMs: 6000, hp: 18, respawnMs: 100000, xp: 32 },
    block_void:  { name: '数据块虚空', job: 'block_void',  tier: 6, unlock: 31, ore: 'void_ore',   tickMs: 7200, hp: 20, respawnMs: 140000, xp: 55 },
  };
  // 煤脉排最前——先有煤才炼得出第一块锭，列表顺序就是引导顺序。
  var VEIN_KEYS = ['burn_seam', 'heap_seam', 'fsm_seam', 'vm_seam', 'brin_seam', 'tblspc_deep', 'block_void'];

  var SMELT_XP = { 1: 3, 2: 7, 3: 14, 4: 25, 5: 44, 6: 75 };
  var OFFLINE_CAP = 2000;

  function veinUnlocked(key, level) {
    var v = VEINS[key];
    return !!v && (level || 0) >= v.unlock;
  }

  // agi 缩短挖矿间隔，最多减半（agi 100 封顶）。stats 由调用方从 Combat.playerStats 算好传入。
  function tickMs(key, stats) {
    var v = VEINS[key]; if (!v) return 0;
    var agi = (stats && stats.agi) || 0;
    return Math.round(v.tickMs * (1 - Math.min(0.5, agi * 0.005)));
  }

  // luk 触发双产，最多 50%（luk 125 封顶）。与 combat.js 里 luk 影响掉落的用法一致。
  function yieldPer(stats, rng) {
    rng = rng || Math.random;
    var luk = (stats && stats.luk) || 0;
    return rng() < Math.min(0.5, luk * 0.004) ? 2 : 1;
  }

  return {
    ORES: ORES, BARS: BARS, BAR_KEYS: BAR_KEYS, VEINS: VEINS, VEIN_KEYS: VEIN_KEYS,
    COAL_KEY: COAL_KEY, SMELT_XP: SMELT_XP, OFFLINE_CAP: OFFLINE_CAP,
    veinUnlocked: veinUnlocked, tickMs: tickMs, yieldPer: yieldPer,
  };
});
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/mining.test.js`
Expected: PASS，7 tests

- [ ] **Step 5: 提交**

```bash
git add skins/db-console/mining.js test/mining.test.js
git commit -m "feat(mining): 矿脉/矿石/锭数据表与解锁-效率纯函数

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 冶炼配方与校验

**Files:**
- Modify: `skins/db-console/mining.js`
- Test: `test/mining.test.js`

- [ ] **Step 1: 写失败的测试**

在 `test/mining.test.js` 末尾追加：

```js
test('smeltRecipe：同档矿×3 + 煤×档位，未知 key 为 null', () => {
  const r1 = M.smeltRecipe('heap_bar');
  assert.strictEqual(r1.ores.heap_ore, 3);
  assert.strictEqual(r1.coal, 1);
  assert.strictEqual(r1.xp, M.SMELT_XP[1]);
  const r6 = M.smeltRecipe('void_bar');
  assert.strictEqual(r6.ores.void_ore, 3);
  assert.strictEqual(r6.coal, 6);
  assert.ok(r6.xp > r1.xp);
  assert.strictEqual(M.smeltRecipe('nope'), null);
});

test('smeltCheck：矿够煤不够 / 煤够矿不够 / 都够', () => {
  const noCoal = { ores: { heap_ore: 99, burn_seg: 0 } };
  const c1 = M.smeltCheck('heap_bar', noCoal);
  assert.strictEqual(c1.oresOk, true);
  assert.strictEqual(c1.coalOk, false);
  assert.strictEqual(c1.ok, false);

  const noOre = { ores: { heap_ore: 2, burn_seg: 99 } };
  const c2 = M.smeltCheck('heap_bar', noOre);
  assert.strictEqual(c2.oresOk, false);
  assert.strictEqual(c2.coalOk, true);
  assert.strictEqual(c2.ok, false);

  const rich = { ores: { heap_ore: 3, burn_seg: 1 } };
  assert.strictEqual(M.smeltCheck('heap_bar', rich).ok, true, '刚好够也算够');
});

test('smeltCheck：空存档不崩', () => {
  assert.strictEqual(M.smeltCheck('heap_bar', {}).ok, false);
  assert.strictEqual(M.smeltCheck('nope', { ores: {} }), null);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/mining.test.js`
Expected: FAIL，报 `M.smeltRecipe is not a function`

- [ ] **Step 3: 写实现**

在 `skins/db-console/mining.js` 的 `yieldPer` 之后、`return` 之前插入：

```js
  // 配方由档位统一派生，不逐条写死：1 锭 = 同档矿 × 3 + 煤 × 档位。
  function smeltRecipe(barKey) {
    var b = BARS[barKey]; if (!b) return null;
    var ores = {};
    ores[b.ore] = 3;
    return { ores: ores, coal: b.tier, xp: SMELT_XP[b.tier] };
  }

  function smeltCheck(barKey, state) {
    var r = smeltRecipe(barKey); if (!r) return null;
    var have = (state && state.ores) || {};
    var oresOk = true;
    for (var o in r.ores) if ((have[o] || 0) < r.ores[o]) oresOk = false;
    var coalOk = (have[COAL_KEY] || 0) >= r.coal;
    return { recipe: r, oresOk: oresOk, coalOk: coalOk, ok: oresOk && coalOk };
  }
```

并把 `return` 里的导出补上：

```js
    veinUnlocked: veinUnlocked, tickMs: tickMs, yieldPer: yieldPer,
    smeltRecipe: smeltRecipe, smeltCheck: smeltCheck,
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/mining.test.js`
Expected: PASS，10 tests

- [ ] **Step 5: 提交**

```bash
git add skins/db-console/mining.js test/mining.test.js
git commit -m "feat(mining): 冶炼配方与校验——同档矿×3 + 煤×档位

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 挂机结算 settleMining

**Files:**
- Modify: `skins/db-console/mining.js`
- Test: `test/mining.test.js`

- [ ] **Step 1: 写失败的测试**

在 `test/mining.test.js` 末尾追加。注意 `stats` 传 `{agi:0, luk:0}` 让间隔取基线、产出恒为 1，结果才可断言：

```js
const FLAT = { agi: 0, luk: 0 };          // 无加成：间隔取基线，产出恒 1
const NEVER = () => 0.99;                 // rng 恒不命中双产

test('settleMining：按间隔产矿并给 xp，不足一格的时间保留到下次', () => {
  const v = M.VEINS.heap_seam;            // tickMs 3000, hp 10
  const mine = { vein: 'heap_seam', last: 0, hp: v.hp, until: 0 };
  const out = M.settleMining(mine, FLAT, 3000 * 3 + 500, NEVER);
  assert.strictEqual(out.ticks, 3);
  assert.strictEqual(out.ores.heap_ore, 3);
  assert.strictEqual(out.xp, v.xp * 3);
  assert.strictEqual(out.mine.hp, v.hp - 3);
  assert.strictEqual(out.mine.last, 9000, '余下 500ms 不结算，留到下次');
});

test('settleMining 不改入参（纯函数）', () => {
  const mine = { vein: 'heap_seam', last: 0, hp: 10, until: 0 };
  M.settleMining(mine, FLAT, 60000, NEVER);
  assert.strictEqual(mine.last, 0);
  assert.strictEqual(mine.hp, 10);
});

test('settleMining：挖空进再生 CD，CD 内不产出，CD 满自动补满耐久', () => {
  const v = M.VEINS.heap_seam;            // hp 10, tickMs 3000, respawn 20000
  const mine = { vein: 'heap_seam', last: 0, hp: v.hp, until: 0 };
  // 刚好挖空：10 次 × 3000ms
  const dug = M.settleMining(mine, FLAT, 30000, NEVER);
  assert.strictEqual(dug.ticks, 10);
  assert.strictEqual(dug.mine.hp, 0);

  // CD 中途：不再产出
  const inCd = M.settleMining(dug.mine, FLAT, 30000 + 10000, NEVER);
  assert.strictEqual(inCd.ticks, 0, '再生中不产出');
  assert.ok(inCd.mine.until > 30000 + 10000, '仍在 CD 内');

  // CD 结束后再挖一格：50000 = 挖空(30000) + CD(20000)，之后 3000ms 出一格
  const after = M.settleMining(dug.mine, FLAT, 30000 + 20000 + 3000, NEVER);
  assert.strictEqual(after.ticks, 1, 'CD 结束后恢复开采');
  assert.strictEqual(after.mine.hp, v.hp - 1, '再生后耐久补满再扣 1');
  assert.strictEqual(after.mine.until, 0, 'CD 已清');
});

test('settleMining：luk 双产写进产出', () => {
  const mine = { vein: 'heap_seam', last: 0, hp: 10, until: 0 };
  const out = M.settleMining(mine, { agi: 0, luk: 9999 }, 3000 * 2, () => 0);
  assert.strictEqual(out.ticks, 2);
  assert.strictEqual(out.ores.heap_ore, 4, '两格各双产');
});

test('settleMining：超长离线被 OFFLINE_CAP 截断且不死循环', () => {
  const mine = { vein: 'heap_seam', last: 0, hp: 10, until: 0 };
  const out = M.settleMining(mine, FLAT, 1000 * 60 * 60 * 24 * 30, NEVER);  // 30 天
  assert.ok(out.ticks <= M.OFFLINE_CAP, '产出不得超过上限');
  assert.ok(out.ticks > 0, '应该有产出');
  assert.strictEqual(out.mine.last, 1000 * 60 * 60 * 24 * 30, '截断后 last 跳到 now，不留补偿');
});

test('settleMining：vein 为 null 或未知 key 时优雅无操作', () => {
  const none = M.settleMining({ vein: null, last: 0, hp: 0, until: 0 }, FLAT, 99999, NEVER);
  assert.strictEqual(none.ticks, 0);
  assert.deepStrictEqual(none.ores, {});
  assert.strictEqual(none.mine.last, 99999, 'last 仍推进，避免下次重复结算');

  const bad = M.settleMining({ vein: 'corrupted', last: 0, hp: 5, until: 0 }, FLAT, 99999, NEVER);
  assert.strictEqual(bad.ticks, 0, '损坏的存档不能让每秒 render 崩掉');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/mining.test.js`
Expected: FAIL，报 `M.settleMining is not a function`

- [ ] **Step 3: 写实现**

在 `skins/db-console/mining.js` 的 `smeltCheck` 之后插入：

```js
  // 挂机结算：从 mine.last 推进到 now，模拟「挖满耐久 → 进再生 CD → 补满 → 继续挖」。
  // 纯函数：不改入参，返回产出增量与新的 mine 状态，由调用方落到 state 上。
  function settleMining(mine, stats, now, rng) {
    rng = rng || Math.random;
    var out = {
      ores: {}, xp: 0, ticks: 0,
      mine: { vein: mine.vein, last: mine.last, hp: mine.hp, until: mine.until },
    };
    var v = VEINS[mine.vein];
    // vein 为空、或存档里 key 损坏 → 优雅无操作。last 仍推进，否则下次又从头算一遍。
    if (!v) { out.mine.last = now; return out; }
    var step = tickMs(mine.vein, stats);
    if (step <= 0) { out.mine.last = now; return out; }

    var t = mine.last || now, hp = mine.hp, until = mine.until || 0;
    var guard = 0;
    while (t < now && guard < OFFLINE_CAP) {
      guard++;
      if (until > t) {                    // 再生中
        if (until > now) { t = now; break; }
        t = until; until = 0; hp = v.hp;  // CD 结束，耐久补满
        continue;
      }
      if (hp <= 0) { until = t + v.respawnMs; continue; }
      if (t + step > now) break;          // 这一格还没挖完，余数留到下次
      t += step;
      var n = yieldPer(stats, rng);
      out.ores[v.ore] = (out.ores[v.ore] || 0) + n;
      out.xp += v.xp;
      out.ticks++;
      hp--;
    }
    // 超过上限的部分直接丢弃（对齐 combat 的离线截断），否则回来后会连着好几秒疯狂补矿。
    out.mine.last = (guard >= OFFLINE_CAP) ? now : t;
    out.mine.hp = hp;
    out.mine.until = until;
    return out;
  }
```

并把 `return` 里的导出补上：

```js
    smeltRecipe: smeltRecipe, smeltCheck: smeltCheck, settleMining: settleMining,
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/mining.test.js`
Expected: PASS，16 tests

- [ ] **Step 5: 提交**

```bash
git add skins/db-console/mining.js test/mining.test.js
git commit -m "feat(mining): 挂机结算——耐久-再生循环、离线截断、存档损坏兜底

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 存档迁移 migrateMining

**Files:**
- Modify: `skins/db-console/mining.js`
- Test: `test/mining.test.js`

- [ ] **Step 1: 写失败的测试**

在 `test/mining.test.js` 末尾追加：

```js
test('migrateMining：补默认字段，不覆盖已有数据', () => {
  const s = { coins: 5 };
  M.migrateMining(s, 1000);
  assert.deepStrictEqual(s.ores, {});
  assert.deepStrictEqual(s.bars, {});
  assert.deepStrictEqual(s.mine, { vein: null, last: 1000, hp: 0, until: 0 });
  assert.strictEqual(s.stats.mined, 0);
  assert.strictEqual(s.coins, 5, '不动无关字段');

  const s2 = { ores: { heap_ore: 7 }, bars: { fsm_bar: 2 },
    mine: { vein: 'heap_seam', last: 500, hp: 4, until: 0 }, stats: { taps: 9 } };
  M.migrateMining(s2, 1000);
  assert.strictEqual(s2.ores.heap_ore, 7);
  assert.strictEqual(s2.bars.fsm_bar, 2);
  assert.strictEqual(s2.mine.hp, 4, '不重置进行中的开采');
  assert.strictEqual(s2.mine.last, 500);
  assert.strictEqual(s2.stats.taps, 9, '不覆盖已有统计');
  assert.strictEqual(s2.stats.mined, 0, '补上缺的统计项');
});

test('migrateMining：老存档的 oreCount 折成铜矿后清零', () => {
  const s = { oreCount: 3 };
  M.migrateMining(s, 1000);
  assert.strictEqual(s.ores.heap_ore, 3);
  assert.strictEqual(s.oreCount, 0);
});

test('migrateMining 幂等：重复调用不重复转换、不重置进度', () => {
  const s = { oreCount: 3 };
  M.migrateMining(s, 1000);
  s.mine = { vein: 'heap_seam', last: 800, hp: 6, until: 0 };
  s.ores.heap_ore += 5;
  M.migrateMining(s, 2000);
  assert.strictEqual(s.ores.heap_ore, 8, '不得二次转换 oreCount');
  assert.strictEqual(s.mine.hp, 6, '不得重置进行中的开采');
  assert.strictEqual(s.mine.last, 800);
});

test('migrateMining：mine 字段被写坏也能修回来', () => {
  const s = { mine: { vein: 'heap_seam' } };
  M.migrateMining(s, 1000);
  assert.strictEqual(s.mine.last, 1000);
  assert.strictEqual(s.mine.hp, 0);
  assert.strictEqual(s.mine.until, 0);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/mining.test.js`
Expected: FAIL，报 `M.migrateMining is not a function`

- [ ] **Step 3: 写实现**

在 `skins/db-console/mining.js` 的 `settleMining` 之后插入：

```js
  function migrateMining(s, now) {
    if (!s.ores || typeof s.ores !== 'object') s.ores = {};
    if (!s.bars || typeof s.bars !== 'object') s.bars = {};
    if (!s.mine || typeof s.mine !== 'object') s.mine = { vein: null, last: now, hp: 0, until: 0 };
    if (!('vein' in s.mine)) s.mine.vein = null;
    if (typeof s.mine.last !== 'number') s.mine.last = now;
    if (typeof s.mine.hp !== 'number') s.mine.hp = 0;
    if (typeof s.mine.until !== 'number') s.mine.until = 0;
    if (!s.stats || typeof s.stats !== 'object') s.stats = {};
    if (typeof s.stats.mined !== 'number') s.stats.mined = 0;
    // oreCount 是开采做出来之前的占位字段（恒 0）。万一有值，折成等量一档铜矿后清零；
    // 清零让这一步天然幂等，此后该字段不再被任何代码读写。
    if (typeof s.oreCount === 'number' && s.oreCount > 0) {
      s.ores.heap_ore = (s.ores.heap_ore || 0) + s.oreCount;
      s.oreCount = 0;
    }
  }
```

并把 `return` 里的导出补上：

```js
    settleMining: settleMining, migrateMining: migrateMining,
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/mining.test.js`
Expected: PASS，20 tests

- [ ] **Step 5: 提交**

```bash
git add skins/db-console/mining.js test/mining.test.js
git commit -m "feat(mining): 存档迁移——补字段、oreCount 折算铜矿、幂等

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: equip.js 配方从矿石改吃锭

**Files:**
- Modify: `skins/db-console/equip.js`
- Test: `test/equip.test.js`

- [ ] **Step 1: 改测试（这次是先改断言，让它失败）**

在 `test/equip.test.js` 顶部的 require 区加一行：

```js
const M = require('../skins/db-console/mining.js');
```

把 `recipe：按档派生材料/矿石/金币/等级` 整个测试替换为：

```js
test('recipe：按档派生材料/锭/金币/等级', () => {
  const r = E.recipe('wpn1'); // T1
  assert.ok(r.mats.page_scrap > 0);
  assert.strictEqual(r.bars[E.TIER_BAR[1]], 2, 'T1 需 1+tier = 2 锭');
  assert.strictEqual(r.level, 1);
  const r6 = E.recipe('wpn6');
  assert.ok(r6.coins > r.coins);
  assert.strictEqual(r6.bars[E.TIER_BAR[6]], 7, 'T6 需 7 锭');
  assert.ok(!('ore' in r), 'ore 字段已废弃');
});
```

把 `recipeCheck：矿石恒缺 → ok 永远 false` 整个测试替换为：

```js
test('recipeCheck：锭不够则锁，锭够则可锻', () => {
  const noBars = { coins: 1e9, mats: { page_scrap: 999 }, bars: {} };
  const c = E.recipeCheck('wpn1', noBars, 99);
  assert.strictEqual(c.barsOk, false);
  assert.strictEqual(c.ok, false);
  assert.strictEqual(c.matsOk, true);   // 材料/金币/等级都够，只差锭
  assert.strictEqual(c.coinsOk, true);
  assert.strictEqual(c.levelOk, true);

  const rich = { coins: 1e9, mats: { page_scrap: 999 }, bars: { heap_bar: 2 } };
  const c2 = E.recipeCheck('wpn1', rich, 99);
  assert.strictEqual(c2.barsOk, true);
  assert.strictEqual(c2.ok, true, '锭补齐后锻造解锁');

  const lowLevel = E.recipeCheck('wpn6', { coins: 1e9, mats: { txn_soul: 999 }, bars: { void_bar: 99 } }, 1);
  assert.strictEqual(lowLevel.levelOk, false);
  assert.strictEqual(lowLevel.ok, false);
});

test('TIER_BAR 的 6 个 key 与 Mining.BARS 完全一致', () => {
  for (let t = 1; t <= 6; t++) {
    const bk = E.TIER_BAR[t];
    assert.ok(M.BARS[bk], 'T' + t + ' 的锭 ' + bk + ' 不在 Mining.BARS 里');
    assert.strictEqual(M.BARS[bk].tier, t, bk + ' 档位错位');
  }
  assert.strictEqual(Object.keys(E.TIER_BAR).length, M.BAR_KEYS.length);
});
```

在 `migrateEquip 补默认、不丢旧数据` 里，删掉两行 `oreCount` 断言：

```js
  assert.strictEqual(s.oreCount, 0);          // ← 删除这一行
```

以及 `s2` 那段里的：

```js
  assert.strictEqual(s2.oreCount, 3);         // ← 删除这一行
```

`s2` 的字面量里 `oreCount: 3` 也一并删掉（迁移职责已移交 `Mining.migrateMining`）。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/equip.test.js`
Expected: FAIL，报 `E.TIER_BAR` 为 undefined、`c.barsOk` 为 undefined

- [ ] **Step 3: 改实现**

在 `skins/db-console/equip.js` 里，`TIER_MAT` / `TIER_UNLOCK` 那两行之后加：

```js
  // 档位 → 锭。key 必须与 Mining.BARS 对齐（equip 不 require mining，靠测试保证一致）。
  var TIER_BAR = { 1: 'heap_bar', 2: 'fsm_bar', 3: 'vm_bar', 4: 'brin_bar', 5: 'tblspc_bar', 6: 'void_bar' };
```

把 `recipe` 替换为：

```js
  function recipe(key) {
    var e = EQUIP[key]; if (!e) return null;
    var t = e.tier, mats = {}, bars = {};
    mats[TIER_MAT[t]] = 4 + t * 2;
    bars[TIER_BAR[t]] = 1 + t;
    return { mats: mats, bars: bars, coins: 40 * t * t, level: TIER_UNLOCK[t] };
  }
```

把 `recipeCheck` 替换为（注意把上方那句「oreOk 因本期无矿石来源恒为 false」的注释一并换掉）：

```js
  // 配方可否满足。锭由开采→冶炼产出（见 mining.js）。
  function recipeCheck(key, state, level) {
    var r = recipe(key); if (!r) return null;
    var matsOk = true;
    for (var m in r.mats) if (((state.mats || {})[m] || 0) < r.mats[m]) matsOk = false;
    var barsOk = true;
    for (var b in r.bars) if (((state.bars || {})[b] || 0) < r.bars[b]) barsOk = false;
    var levelOk = level >= r.level;
    var coinsOk = (state.coins || 0) >= r.coins;
    return { recipe: r, matsOk: matsOk, levelOk: levelOk, coinsOk: coinsOk, barsOk: barsOk,
      ok: matsOk && levelOk && coinsOk && barsOk };
  }
```

在 `migrateEquip` 里删掉这一行（职责移交 `Mining.migrateMining`）：

```js
    if (typeof s.oreCount !== 'number') s.oreCount = 0;   // ← 删除
```

在导出里，`TIER_MAT: TIER_MAT, TIER_UNLOCK: TIER_UNLOCK,` 后面加 `TIER_BAR: TIER_BAR,`。

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/equip.test.js test/mining.test.js`
Expected: PASS，equip 10 tests + mining 20 tests

- [ ] **Step 5: 提交**

```bash
git add skins/db-console/equip.js test/equip.test.js
git commit -m "refactor(equip): 配方从 oreCount 改吃锭——bars 与 mats 同形

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: 模块接线（服务器白名单 + 两个皮肤）

`mining.js` 必须像 equip.js 一样进 `GAME_FILES` 白名单，否则 tracer 皮肤 404；tracer 的 index.html 也必须引入它，否则 Garden 里 farm.js 会因 `Mining is not defined` 直接崩。

**Files:**
- Modify: `server.js:55`
- Modify: `skins/db-console/index.html:67`
- Modify: `skins/tracer/index.html:171`
- Test: `test/server.test.js:178`, `test/tracer-skin.test.js:62`

- [ ] **Step 1: 先确认工作区干净（tracer 那条线也在改这些文件）**

Run: `git status --short`
Expected: `server.js`、`skins/tracer/index.html`、`test/tracer-skin.test.js` 均无 `M` 标记。若有，先与用户确认再动。

- [ ] **Step 2: 写失败的测试**

`test/server.test.js:178`，把兜底文件列表加一项：

```js
  for (const p of ['/farm.js', '/fishing.js', '/combat.js', '/equip.js', '/mining.js', '/farm.css']) {
```

`test/tracer-skin.test.js` 的 `Garden 的游戏引擎脚本齐全且顺序正确` 测试，改标题并补断言：

```js
test('Garden 的游戏引擎脚本齐全且顺序正确（fishing→combat→equip→mining→farm，均在 app.js 之后）', async () => {
  const html = (await get('/')).body;
  const idxApp = html.indexOf('/app.js');
  const idxFishing = html.indexOf('/fishing.js');
  const idxCombat = html.indexOf('/combat.js');
  const idxEquip = html.indexOf('/equip.js');
  const idxMining = html.indexOf('/mining.js');
  const idxFarm = html.indexOf('src="/farm.js"');
  assert.ok(idxApp >= 0, '应引用 app.js');
  assert.ok(idxFishing >= 0, '应引用 fishing.js');
  assert.ok(idxCombat >= 0, '应引用 combat.js');
  assert.ok(idxEquip >= 0, '应引用 equip.js');
  assert.ok(idxMining >= 0, '应引用 mining.js');
  assert.ok(idxFarm >= 0, '应引用 farm.js');
  // boss-key 依赖 app.js 的 keydown 处理器先于 farm.js 的注册，顺序错了就会失效
  assert.ok(idxApp < idxFishing, 'app.js 必须先于 fishing.js（boss-key 先注册）');
  assert.ok(idxFishing < idxCombat, 'fishing.js 必须先于 combat.js');
  assert.ok(idxCombat < idxEquip, 'combat.js 必须先于 equip.js');
  assert.ok(idxEquip < idxMining, 'equip.js 必须先于 mining.js');
  assert.ok(idxMining < idxFarm, 'mining.js 必须先于 farm.js');
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `node --test test/server.test.js test/tracer-skin.test.js`
Expected: FAIL，`/mining.js 应兜底服务` 与 `应引用 mining.js` 两条不通过

- [ ] **Step 4: 写实现**

`server.js:55` 的 `GAME_FILES`：

```js
const GAME_FILES = new Set([
  '/farm.js', '/fishing.js', '/combat.js', '/equip.js', '/mining.js', '/farm-data.js', '/farm.css',
]);
```

`skins/db-console/index.html`，在 `/equip.js` 与 `/farm.js` 之间插入：

```html
<script src="/mining.js"></script>
```

`skins/tracer/index.html`，同样在 `/equip.js` 与 `/farm.js` 之间插入：

```html
<script src="/mining.js"></script>
```

- [ ] **Step 5: 跑全量测试确认通过**

Run: `node --test test/*.test.js`
Expected: PASS，0 fail

- [ ] **Step 6: 提交**

```bash
git add server.js skins/db-console/index.html skins/tracer/index.html test/server.test.js test/tracer-skin.test.js
git commit -m "feat(server): mining.js 进游戏资源白名单，两个皮肤同步引入

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: farm.js 状态、迁移与每秒结算

此任务结束后开采已经在后台真实产矿，只是还没有界面看。

**Files:**
- Modify: `skins/db-console/farm.js`

- [ ] **Step 1: 加状态字段**

`freshState()` 里，把这一行：

```js
      owned: [], oreCount: 0,
```

改为：

```js
      owned: [],
      ores: {}, bars: {}, mine: { vein: null, last: Date.now(), hp: 0, until: 0 },
```

同一函数里 `stats` 那行补上 `mined`：

```js
      stats: { taps: 0, harvested: 0, earned: 0, orders: 0, fish: 0, ext: 0, mined: 0 },
```

- [ ] **Step 2: 接上迁移**

`load()` 末尾，`Equip.migrateEquip(state);` 之后加：

```js
    // 补齐开采相关字段（新字段、老存档都要走这一步；oreCount 也在这里折算）。
    Mining.migrateMining(state, Date.now());
```

- [ ] **Step 3: 每秒 render 里结算**

`render()` 里，在 `if (settleGrow(now)) dirty = true;` 之后、`var rc = Fishing.settleReclaim(` 之前插入：

```js
    // 开采挂机结算：分钟级节奏，跟着每秒 render 走即可，不像战斗那样另开定时器。
    if (state.mine.vein) {
      var mps = Combat.playerStats(levelInfo().lvl, Equip.equipBonus(state.equipped));
      var mo = Mining.settleMining(state.mine, mps, now);
      if (mo.ticks > 0) {
        for (var mok in mo.ores) state.ores[mok] = (state.ores[mok] || 0) + mo.ores[mok];
        state.xp += mo.xp;
        state.stats.mined += mo.ticks;
        dirty = true;
      }
      state.mine = mo.mine;
    }
```

- [ ] **Step 4: 加 tab 与占位视图**

`TABS` 里，在 `raid` 与 `inv` 之间插入：

```js
    { id: 'mine',  job: 'storage',     name: '开采' },
```

`render()` 的 `body` 三元链里，在 `: state.tab === 'inv' ? invTab()` 之前插入：

```js
      : state.tab === 'mine' ? mineTab()
```

在 `forgeTab()` 之前加占位实现（Task 8 会替换成完整版）：

```js
  // 开采：矿脉列表（选中即挂机）+ 矿石库存。冶炼在锻造 tab。
  function mineTab() {
    return '<div class="pad"><div class="kb-hint">'
      + (state.real ? '开采界面建设中。' : 'mining ui wip.') + '</div></div>';
  }
```

- [ ] **Step 5: 跑全量测试 + 手工验证后台真在产矿**

先确认 farm.js 里再无别处引用 `oreCount`：

Run: `grep -n oreCount skins/db-console/farm.js`
Expected: 无输出（Step 1 删掉的是唯一一处）

Run: `node --test test/*.test.js`
Expected: PASS，0 fail

Run: `DOCS_PORTAL_PORT=8791 DOCS_PORTAL_SKIN=db-console node server.js`

浏览器开 `http://127.0.0.1:8791/`。此时还没有选矿脉的界面（Task 8 才做），所以在 F12 控制台直接改存档挂上煤脉，验证后台真的在产矿：

```js
var K = 'dbconsole.farm.v3';
var s = JSON.parse(localStorage.getItem(K));
console.log('字段齐了吗:', !!s.ores, !!s.bars, s.mine);   // 应为 true true {vein: null, ...}
s.mine = { vein: 'burn_seam', last: Date.now(), hp: 12, until: 0 };
localStorage.setItem(K, JSON.stringify(s));
location.reload();
```

等约 20 秒（煤脉基线 3.2s 一格）后再在控制台执行：

```js
var s = JSON.parse(localStorage.getItem('dbconsole.farm.v3'));
console.log('煤:', s.ores.burn_seg, ' 挖了几格:', s.stats.mined, ' 耐久:', s.mine.hp);
```

Expected: 煤 > 0、挖了几格 > 0、耐久从 12 开始递减

- [ ] **Step 6: 提交**

```bash
git add skins/db-console/farm.js
git commit -m "feat(mining): farm 接线——状态字段、存档迁移、每秒挂机结算

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: 开采 tab 界面与选矿脉交互

**Files:**
- Modify: `skins/db-console/farm.js`
- Modify: `skins/db-console/farm.css`

- [ ] **Step 1: 加图标辅助函数**

在 `farm.js` 的 `matFace` 函数之后加（本期矿石不出图，只给 emoji；伪装模式什么都不显示，与 `matFace` 行为一致）：

```js
  // 开采本期不出图，真实模式给 emoji、伪装模式留空——和 matFace 的伪装行为保持一致。
  function oreFace(key) {
    var o = Mining.ORES[key];
    return (state.real && o) ? o.emoji + ' ' : '';
  }
  function barFace(key) {
    var b = Mining.BARS[key];
    return (state.real && b) ? b.emoji + ' ' : '';
  }
```

- [ ] **Step 2: 用完整实现替换 Task 7 的 mineTab 占位**

```js
  // 开采：矿脉列表（选中即挂机）+ 矿石库存。冶炼在锻造 tab。
  function mineTab() {
    var lvl = levelInfo().lvl;
    var ps = Combat.playerStats(lvl, Equip.equipBonus(state.equipped));
    var now = Date.now();
    var rows = Mining.VEIN_KEYS.map(function (k) {
      var v = Mining.VEINS[k];
      if (!Mining.veinUnlocked(k, lvl)) {
        return '<tr class="vein-locked"><td>' + esc(state.real ? '???' : v.job) + '</td>'
          + '<td>' + (state.real ? 'Lv.' : 'lv ') + v.unlock + '</td><td></td><td></td></tr>';
      }
      var cur = state.mine.vein === k;
      var status = '';
      if (cur) {
        status = (state.mine.until > now)
          ? '<span class="vein-cd">' + (state.real ? '再生中 ' : 'respawn ')
              + Math.ceil((state.mine.until - now) / 1000) + 's</span>'
          : hpBar(state.mine.hp, v.hp, 'hp-ore');
      }
      return '<tr' + (cur ? ' class="is-active"' : '') + '>'
        + '<td>' + oreFace(v.ore) + esc(state.real ? v.name : v.job) + '</td>'
        + '<td>' + (Mining.tickMs(k, ps) / 1000).toFixed(1) + 's</td>'
        + '<td>' + status + '</td>'
        + '<td>' + (cur
            ? '<span class="mini on">' + (state.real ? '开采中' : 'active') + '</span>'
            : '<a class="mini" data-vein="' + k + '">' + (state.real ? '开采' : 'mine') + '</a>')
        + '</td></tr>';
    }).join('');
    var have = Object.keys(Mining.ORES).filter(function (k) { return (state.ores[k] || 0) > 0; })
      .map(function (k) {
        var o = Mining.ORES[k];
        return '<tr><td>' + oreFace(k) + esc(state.real ? o.name : o.job) + '</td>'
          + '<td class="num">' + state.ores[k] + '</td></tr>';
      }).join('');
    return '<div class="pad">'
      + '<div class="kb-hint">' + (state.real
          ? '选一条矿脉挂着自动开采；耐久挖空后自行再生。冶炼在锻造页。'
          : 'pick one seam; it drains and respawns on its own.') + '</div>'
      + '<table class="grid"><tr><th>' + (state.real ? '矿脉' : 'seam') + '</th>'
      + '<th>' + (state.real ? '间隔' : 'every') + '</th>'
      + '<th>' + (state.real ? '耐久' : 'left') + '</th><th></th></tr>' + rows + '</table>'
      + '<div class="rail-label">' + (state.real ? '矿石' : 'objects') + '</div>'
      + (have ? '<table class="grid"><tr><th>' + (state.real ? '矿石' : 'object') + '</th>'
          + '<th>' + (state.real ? '数量' : 'count') + '</th></tr>' + have + '</table>'
        : '<div class="kb-hint">' + (state.real ? '还没有矿石。' : 'no ore yet.') + '</div>')
      + '</div>';
  }
```

- [ ] **Step 3: 加选矿脉动作与点击分发**

在 `unequipSlot` 函数之后加：

```js
  function selectVein(key) {
    if (!Mining.veinUnlocked(key, levelInfo().lvl)) return;
    if (state.mine.vein === key) return;
    // 换矿脉从满耐久重新开始，不继承上一条的耐久与再生 CD。
    state.mine = { vein: key, last: Date.now(), hp: Mining.VEINS[key].hp, until: 0 };
    redraw();
  }
```

在 `onMainClick` 里，`data-spot` 那段之前插入：

```js
    var vn = e.target.closest ? e.target.closest('[data-vein]') : null;
    if (vn) { selectVein(vn.getAttribute('data-vein')); return; }
```

- [ ] **Step 4: 加 CSS**

`skins/db-console/farm.css` 的 `.hpfill.hp-p` 那行之后加：

```css
.hpfill.hp-ore { background: #c99a4a; }
.vein-locked td { opacity: .45; }
.vein-cd { color: #8a8a8a; }
```

- [ ] **Step 5: 跑测试 + 手工验证两种模式**

Run: `node --test test/*.test.js`
Expected: PASS，0 fail

Run: `DOCS_PORTAL_PORT=8791 DOCS_PORTAL_SKIN=db-console node server.js`

浏览器开 `http://127.0.0.1:8791/`：
1. 切到开采 tab，点「燃料层」的 mine 链接 → 该行高亮、出现耐久条
2. 等约 40 秒 → 耐久条走空 → 出现 respawn 倒计时 → 倒计时结束后耐久条重新变满
3. 矿石表里 `burn_seg` 数量在涨
4. 按 Ctrl+Alt+G 切伪装模式 → 整个开采 tab 无中文、无 emoji，只剩黑话

- [ ] **Step 6: 提交**

```bash
git add skins/db-console/farm.js skins/db-console/farm.css
git commit -m "feat(mining): 开采 tab——矿脉列表、耐久条、再生倒计时、选脉交互

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: 熔炉与锻造解禁

**Files:**
- Modify: `skins/db-console/farm.js`
- Modify: `skins/db-console/farm.css`

- [ ] **Step 1: 加冶炼与锻造动作**

在 `selectVein` 之后加：

```js
  function doSmelt(barKey) {
    var c = Mining.smeltCheck(barKey, state);
    if (!c || !c.ok) return;
    for (var o in c.recipe.ores) state.ores[o] -= c.recipe.ores[o];
    state.ores[Mining.COAL_KEY] -= c.recipe.coal;
    state.bars[barKey] = (state.bars[barKey] || 0) + 1;
    state.xp += c.recipe.xp;
    var b = Mining.BARS[barKey];
    toast(state.real ? '炼成' + b.name : 'smelted ' + b.job);
    redraw();
  }

  function forgeItem(key) {
    var c = Equip.recipeCheck(key, state, levelInfo().lvl);
    if (!c || !c.ok) return;
    for (var m in c.recipe.mats) state.mats[m] -= c.recipe.mats[m];
    for (var b in c.recipe.bars) state.bars[b] -= c.recipe.bars[b];
    earn(-c.recipe.coins);
    state.owned.push({ key: key, level: 0 });
    toast(state.real ? '锻成' + Equip.EQUIP[key].name : 'forged ' + Equip.EQUIP[key].job);
    redraw();
  }
```

在 `onMainClick` 里，刚加的 `data-vein` 那段之后插入：

```js
    var sm = e.target.closest ? e.target.closest('[data-smelt]') : null;
    if (sm) { doSmelt(sm.getAttribute('data-smelt')); return; }
    var fg = e.target.closest ? e.target.closest('[data-forge]') : null;
    if (fg) { forgeItem(fg.getAttribute('data-forge')); return; }
```

- [ ] **Step 2: 用带熔炉的版本整体替换 forgeTab**

```js
  // 锻造：上半部熔炉（矿石→锭），下半部装备配方（吃锭）。
  function forgeTab() {
    var lvl = levelInfo().lvl;
    var coal = Mining.ORES[Mining.COAL_KEY];
    var smelt = Mining.BAR_KEYS.map(function (bk) {
      var b = Mining.BARS[bk], c = Mining.smeltCheck(bk, state), o = Mining.ORES[b.ore];
      var need = '<span class="' + (c.oresOk ? '' : 'rec-miss') + '">' + oreFace(b.ore)
          + esc(state.real ? o.name : o.job) + '×' + c.recipe.ores[b.ore] + '</span>'
        + ' · <span class="' + (c.coalOk ? '' : 'rec-miss') + '">' + oreFace(Mining.COAL_KEY)
          + esc(state.real ? coal.name : coal.job) + '×' + c.recipe.coal + '</span>';
      return '<tr><td>' + barFace(bk) + esc(state.real ? b.name : b.job) + '</td>'
        + '<td class="num">' + (state.bars[bk] || 0) + '</td>'
        + '<td class="rec-need">' + need + '</td>'
        + '<td>' + (c.ok
            ? '<a class="mini" data-smelt="' + bk + '">' + (state.real ? '冶炼' : 'smelt') + '</a>'
            : '<button class="btn btn-dim" disabled>' + (state.real ? '冶炼' : 'smelt') + '</button>')
        + '</td></tr>';
    }).join('');
    var rows = Equip.EQUIP_KEYS.map(function (k) {
      var c = Equip.recipeCheck(k, state, lvl), r = c.recipe;
      var need = Object.keys(r.mats).map(function (m) {
        return esc(state.real ? Combat.MATERIALS[m].name : Combat.MATERIALS[m].job) + '×' + r.mats[m];
      }).join(' ');
      var barNeed = Object.keys(r.bars).map(function (b) {
        return barFace(b) + esc(state.real ? Mining.BARS[b].name : Mining.BARS[b].job) + '×' + r.bars[b];
      }).join(' ');
      return '<tr><td>' + equipFace(k) + esc(equipName(k)) + '</td>'
        + '<td>' + (state.real ? 'Lv.' : 'lv ') + r.level + '</td>'
        + '<td class="rec-need"><span class="' + (c.matsOk ? '' : 'rec-miss') + '">' + need + '</span>'
        + ' · <span class="' + (c.barsOk ? '' : 'rec-miss') + '">' + barNeed + '</span>'
        + ' · <span class="' + (c.coinsOk ? '' : 'rec-miss') + '">&#x25C8;' + r.coins + '</span></td>'
        + '<td>' + (c.ok
            ? '<a class="mini" data-forge="' + k + '">' + (state.real ? '锻造' : 'forge') + '</a>'
            : '<button class="btn btn-dim" disabled>' + (state.real ? '锻造' : 'forge') + '</button>')
        + '</td></tr>';
    }).join('');
    return '<div class="pad">'
      + '<div class="rail-label">' + (state.real ? '熔炉' : 'furnace') + '</div>'
      + '<table class="grid"><tr><th>' + (state.real ? '锭' : 'bar') + '</th>'
      + '<th>' + (state.real ? '存量' : 'have') + '</th>'
      + '<th>' + (state.real ? '需求' : 'cost') + '</th><th></th></tr>' + smelt + '</table>'
      + '<div class="rail-label">' + (state.real ? '锻造' : 'smithy') + '</div>'
      + '<table class="grid"><tr><th>' + (state.real ? '产出' : 'item') + '</th>'
      + '<th>' + (state.real ? '等级' : 'lv') + '</th>'
      + '<th>' + (state.real ? '需求' : 'cost') + '</th><th></th></tr>' + rows + '</table></div>';
  }
```

- [ ] **Step 3: 加 CSS 并清掉废弃类**

`skins/db-console/farm.css` 里，把 `.rec-ore` 那条规则整体替换为：

```css
.rec-miss { color: #e0603e; }
```

`.rec-ore` 在 `farm.css:285`，是「矿石待开采」时期的占位样式；Task 9 重写 forgeTab 后已无引用，换成 `.rec-miss`。

- [ ] **Step 4: 跑全量测试**

Run: `node --test test/*.test.js`
Expected: PASS，0 fail

- [ ] **Step 5: 手工走通全闭环**

Run: `DOCS_PORTAL_PORT=8791 DOCS_PORTAL_SKIN=db-console node server.js`

浏览器开 `http://127.0.0.1:8791/`，从零存档（先 `localStorage.clear()` 再刷新）走一遍：
1. 开采 tab 挂「燃料层」，攒到 ≥1 煤
2. 换挂「堆表矿脉」，攒到 ≥3 铜矿
3. 锻造 tab 熔炉区，「铜锭」行的 smelt 变成可点 → 点它 → 铜锭存量 +1、提示「炼成铜锭」
4. 重复到有 2 块铜锭
5. 打怪攒够 `page_scrap` ×6 与 40 金币后，`wpn1`（生锈短刃）行的 forge 变可点 → 点它 → 背包里出现该装备
6. Ctrl+Alt+G 切伪装模式 → 熔炉与锻造两张表全黑话、无中文无 emoji

Expected: 六步全部通过。第 5 步是整个闭环的验收点 —— 这是锻造第一次真正可用。

- [ ] **Step 6: 提交**

```bash
git add skins/db-console/farm.js skins/db-console/farm.css
git commit -m "feat(mining): 熔炉与锻造解禁——矿石炼锭、锭锻装备，闭环打通

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## 完成后

全量 `node --test test/*.test.js` 应为绿，测试数从 233 涨到 254（mining 新增 20 条，equip 净 +1 条）。

spec 里明确留到后续、本期不做的：矿石与锭的立绘（AI webp → ffmpeg → `skins/db-console/mats/`），本期只用 emoji 兜底。

已知的范围外缺口（不在本计划内，需另行决定归属）：`server.js` 的 `isGameAsset()` 只放行 `/fish/` 与 `/scenes/`，不含 `/equip/`、`/mobs/`、`/mats/`，所以 tracer 的 Garden 分区里这三类图会 404 退化成 emoji 兜底。db-console 不受影响。
