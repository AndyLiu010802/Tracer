# 2026-09-25 伙伴原画续补

本轮开始时，420 组动作中有 79 组尚未补齐，共缺 155 张分镜页。继续使用内置 `image_gen`，保持每个动作两张 4×4 透明 PNG、合计 32 张独立原画。

## 闪光樱桃

新增 19 张分镜页，补齐摸摸、音乐、庆祝、休息、专注、微风、伸展、张望、害羞、进食、感谢 11 组动作。闪光樱桃的全部 18 组动作现在均有两页原画。

- 图片：`skins/tracer/garden-art/cherry-shiny-*-v3-p*.png`。
- 提示词与原始生成文件：`dev/companion-original-sources.json`。
- 每页均通过透明通道、空白分隔、完整边界、16 帧独立指纹检查；成组素材通过 32 帧跨页指纹检查。
- 使用 `dev/qa-companion-anatomy.cjs cherry` 展开实际播放器使用的帧，目视复查本轮补齐的 11 组动作，共 352 帧。音乐的首个候选含多余手臂，已弃用并重新生成；采用版本未见多余肢体。
- 全局 `dev/qa-companion-originals.cjs` 检查通过：352 组已接入动作、11,264 帧，无加载、页面顺序、缩放或站位错误。
- 后续细查修正了进食后半段的饼干缺口，使已咬掉部分在后续帧中保持缺失；修正后通过 32 帧跨页检查。旧图与来源记录保存在 `.cache/image-continuation/cherry-eat-before-bite-fix.*`，新图已更新来源索引。

## 霓虹兰

新增 42 张分镜页，补齐普通形态 10 组、闪光形态 11 组动作。两种形态的全部 36 组动作现均有两页原画，已重建并接入图集。

- 所有新图均经逐页目视检查，并通过透明通道、分隔、边界和独立帧指纹检查；图集构建没有拒收项。
- 重画了含多余手臂、动作提前回到起点或饼干缺口消失的候选图；弃用候选未写入来源索引。
- `dev/qa-companion-anatomy.cjs neon_orchid` 展开 36 组、1,152 帧；额外目视检查播放器中的普通音乐、庆祝、休息、进食、感谢，以及闪光进食、伸展、音乐，共 8 组、256 帧。
- 全局 `dev/qa-companion-originals.cjs` 检查通过：373 组已接入动作、11,936 帧，无加载、页面顺序、缩放或站位错误。

此阶段累计新增 61 张分镜页，仍缺 94 张：电光莓普通和闪光各 22 张，晶芯树普通 24 张、闪光 26 张。整个 420 组原画计划尚未全部完成。几何与指纹检查不代替角色肢体、道具和动作连贯性的目视检查。

## 电光莓

普通、闪光形态各新增 22 张分镜页，合计补齐 22 组动作；两种形态的全部 36 组动作现均有两页原画。闪光进食后半段另作定向修正，使饼干缺口在全部 16 帧中保持。

- 新增图片逐页目视检查通过，普通音乐、庆祝后半段曾出现多余胸前手部，已弃用并改用对应前半段作为唯一参考重画。
- 图集构建通过，没有拒收项；`dev/qa-companion-originals.cjs` 检查 385 组、12,320 帧，错误列表为空。
- 在实际播放器展开图中额外检查普通音乐、庆祝、进食和闪光浇水 4 组、128 帧；同时重新展开并检查樱桃修正后的进食 32 帧。
- 后续在播放器展开图中额外检查闪光音乐、伸展、感谢 3 组、96 帧。
- 电光莓全部补齐后，重新检查闪光进食 32 帧；全局检查通过 400 组、12,800 帧，错误列表为空。

## 晶芯树

新增普通形态 24 张、闪光形态 26 张分镜页，补齐 25 组动作；两种形态的全部 36 组动作均有两页原画。普通与闪光音乐后半段中途重复抬手、闪光感谢中途重复低头、休息与专注的多余手臂均已重画；进食后半段保留饼干缺口，浇水的幼苗位于壶嘴下方。

- 在实际播放器展开图中检查普通蹲伏、庆祝、专注 3 组、96 帧，并核对其与已有待机的主体尺寸一致。
- 收尾展开全部 36 组、1,152 帧，额外目视检查普通音乐，以及闪光音乐、休息、专注、浇水、进食、感谢 7 组、224 帧。
- 闭眼休息的颜色识别误把较窄的肚子当成脸，导致显示尺寸偏大。在 `dev/companion-art-calibration.json` 按四张原图的 SHA-256 绑定页级校准，以同形态待机的脸宽和实测主体厚度比例校准；每页保持统一比例，不改原图像素或逐帧缩放。

## 完成核验

本轮共补齐 155 张缺失分镜页、79 组动作。最终 420/420 组、840/840 张、13,440 帧：

- `node dev/companion-original-frames.cjs --complete` 通过，拒收列表为空。
- 图集构建接入全部 324 组花精灵动作，没有缺失或拒收项。
- 尺寸校准后再次运行 `dev/qa-companion-originals.cjs`，420 组、13,440 帧的来源、双页顺序、比例和站位检查通过，错误列表为空。
- 尺寸校准后再次运行 `dev/qa-garden-companion-motion.cjs --complete`，324 组、10,368 帧检查通过；主窗口与桌面预览、专注状态保护及预览不发放奖励均通过。
- 重新展开晶芯树 36 组、1,152 帧，并目视对照普通休息、闪光待机与休息，确认闭眼不再导致显示尺寸翻倍。

所有新素材使用内置 `image_gen` 生成并保存到 `skins/tracer/garden-art/`；逐页提示词与生成源文件记录在 `dev/companion-original-sources.json`。自动几何检查与指纹检查之外，所有新增页均经过目视复核。

## 自定义伙伴生成边界

已撤回临时预览缩小，改为约束后续生成：原图与每格动作要求四边至少 12.5% 透明留白，耳朵、尾巴、手和道具全部计入，统一角色比例。新生成动作验收要求每一帧原始边界及脚底对齐后的边界均保留至少 8% 安全空间；失败沿用现有最多两次重试，已有素材导入不追溯新增门槛。

验证：20 项图像请求测试、25 项动画测试通过；创建器浏览器生命周期检查通过；截图所示过大狗伙伴的实际原图被新验收规则拒收。
